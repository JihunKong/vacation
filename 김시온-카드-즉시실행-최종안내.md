# 🎯 김시온 학생 10레벨 카드 즉시 생성 안내

## ⚡ **가장 빠른 해결책 (30초 안에 완료)**

### 방법 1: 김시온 학생 직접 실행 (추천)
1. 김시온 학생이 `kimsion70823@gmail.com`로 로그인
2. 활동 추가 페이지에서 아무 활동 1분 추가:
   - 카테고리: "학습"
   - 제목: "카드 생성용 임시 활동"
   - 시간: 1분
3. **저장 시 자동으로 레벨 체크되어 10레벨 카드 즉시 생성!** ✨

### 방법 2: 서버 직접 명령어 실행
프로덕션 서버에 접속 후 아래 명령어 복사해서 실행:

```bash
cd /home/ubuntu/vacation && DATABASE_URL="postgresql://postgres:postgres@localhost:5432/studylog_db" node -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
(async () => {
  try {
    const user = await prisma.user.findUnique({
      where: { email: 'kimsion70823@gmail.com' },
      include: { studentProfile: { include: { levelImages: true } } }
    });
    
    if (user?.studentProfile) {
      // 기존 10레벨 카드 삭제
      const existing = await prisma.levelImage.findFirst({
        where: { studentId: user.studentProfile.id, level: 10 }
      });
      if (existing) await prisma.levelImage.delete({ where: { id: existing.id } });
      
      // 새 카드 생성
      await prisma.levelImage.create({
        data: {
          studentId: user.studentProfile.id,
          level: 10,
          imageUrl: '/level-cards/kim-sion-level-10-1757486919979.svg',
          prompt: 'Apple Scholar Level 10 Card',
          strength: user.studentProfile.strength,
          intelligence: user.studentProfile.intelligence,
          dexterity: user.studentProfile.dexterity,
          charisma: user.studentProfile.charisma,
          vitality: user.studentProfile.vitality,
          totalXP: user.studentProfile.totalXP,
          totalMinutes: user.studentProfile.totalMinutes
        }
      });
      
      console.log('🎉 김시온 10레벨 카드 생성 완료!');
      console.log('💡 갤러리에서 확인 가능!');
    }
    
    await prisma.\$disconnect();
  } catch (error) {
    console.error('오류:', error);
  }
})();
"
```

## 🎨 **생성될 카드 특징**
- **캐릭터**: Apple Scholar (귀여운 사과 학자 캐릭터)
- **테마**: 한국 웹툰 스타일, 브론즈 색상
- **능력치**: 김시온 학생의 실제 능력치 반영 (INT: 127)
- **스타일**: 다양한 캐릭터 생성 시스템 적용

## ✅ **확인 방법**
카드 생성 후 다음 위치에서 확인:
- 갤러리 페이지: `/dashboard/gallery`
- 프로필 페이지의 카드 섹션

## 🔄 **향후 개선**
- 임시 SVG 카드 → 실제 Gemini AI 생성 이미지로 교체 가능
- 더 많은 캐릭터 타입 추가 (동물, 과일, 사물 등)

## 📞 **문제 발생 시**
카드가 생성되지 않으면 다음 확인:
1. 김시온 계정이 정말 `kimsion70823@gmail.com`인지 확인
2. 현재 레벨이 10 이상인지 확인
3. 기존 10레벨 카드가 있는지 확인

**김시온 학생이 로그인해서 1분 활동만 추가하면 즉시 해결됩니다!** 🎊