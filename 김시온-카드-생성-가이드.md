# 김시온 학생 10레벨 카드 생성 가이드

## 📋 상황 요약
- **활성 계정**: `kimsion70823@gmail.com` (김시온 학생 실제 사용)
- **문제**: 레벨 16임에도 10레벨 카드가 생성되지 않음
- **해결책**: 아래 방법 중 하나로 즉시 해결 가능

## 🚀 즉시 실행 방법

### 방법 1: 프로덕션 서버에서 Node.js 스크립트 실행 (권장)

서버에 접속하여 다음 명령어를 실행하세요:

```bash
cd /home/ubuntu/vacation

# 김시온 카드 생성 스크립트 실행
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/studylog_db" node -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

(async () => {
  try {
    console.log('🔍 김시온 학생 찾는 중...');
    
    const user = await prisma.user.findUnique({
      where: { email: 'kimsion70823@gmail.com' },
      include: { 
        studentProfile: { 
          include: { levelImages: true } 
        } 
      }
    });
    
    if (!user?.studentProfile) {
      console.log('❌ 김시온 학생을 찾을 수 없습니다.');
      return;
    }
    
    console.log(\`✅ 김시온 학생 확인: \${user.name} (레벨 \${user.studentProfile.level})\`);
    
    // 기존 10레벨 카드 삭제 (있다면)
    const existing = await prisma.levelImage.findFirst({
      where: { studentId: user.studentProfile.id, level: 10 }
    });
    
    if (existing) {
      await prisma.levelImage.delete({ where: { id: existing.id } });
      console.log('🗑️ 기존 10레벨 카드 삭제 완료');
    }
    
    // 새 카드 생성
    const newCard = await prisma.levelImage.create({
      data: {
        studentId: user.studentProfile.id,
        level: 10,
        imageUrl: '/level-cards/kim-sion-level-10-1757486919979.svg',
        prompt: 'Apple Scholar Level 10 - cute apple character with magical academic accessories, Korean webtoon style',
        strength: user.studentProfile.strength,
        intelligence: user.studentProfile.intelligence,
        dexterity: user.studentProfile.dexterity,
        charisma: user.studentProfile.charisma,
        vitality: user.studentProfile.vitality,
        totalXP: user.studentProfile.totalXP,
        totalMinutes: user.studentProfile.totalMinutes
      }
    });
    
    console.log('🎉 김시온 10레벨 카드 생성 완료!');
    console.log(\`📋 카드 ID: \${newCard.id}\`);
    console.log('💡 갤러리에서 확인 가능합니다!');
    
  } catch (error) {
    console.error('💥 오류:', error);
  } finally {
    await prisma.\$disconnect();
  }
})();
"
```

### 방법 2: 데이터베이스 직접 SQL 삽입

```bash
# PostgreSQL에 직접 접속하여 SQL 실행
docker exec -it studylog_postgres psql -U postgres -d studylog_db

# 아래 SQL 실행:
INSERT INTO "LevelImage" (
  id,
  "studentId", 
  level,
  "imageUrl",
  prompt,
  strength,
  intelligence,
  dexterity,
  charisma,
  vitality,
  "totalXP",
  "totalMinutes",
  "createdAt",
  "updatedAt"
) VALUES (
  'kim-sion-level-10-manual',
  (SELECT sp.id FROM "StudentProfile" sp 
   JOIN "User" u ON sp."userId" = u.id 
   WHERE u.email = 'kimsion70823@gmail.com'),
  10,
  '/level-cards/kim-sion-level-10-1757486919979.svg',
  'Apple Scholar Level 10 Card - Korean webtoon style',
  (SELECT strength FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT intelligence FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT dexterity FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT charisma FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT vitality FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT "totalXP" FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  (SELECT "totalMinutes" FROM "StudentProfile" sp JOIN "User" u ON sp."userId" = u.id WHERE u.email = 'kimsion70823@gmail.com'),
  NOW(),
  NOW()
);
```

### 방법 3: 김시온 학생에게 임시 활동 추가

김시온 학생이 로그인해서 짧은 활동(1분)을 추가하면 레벨업 이벤트가 트리거되어 자동으로 카드가 생성됩니다.

## 📁 준비된 파일들

다음 파일들이 이미 `/public/level-cards/` 폴더에 준비되어 있습니다:

1. `kim-sion-level-10-1757486919979.svg` - 임시 카드 이미지
2. `kim-sion-level-10-prompt-1757486919979.txt` - Gemini AI 프롬프트
3. `kim-sion-level-10-insert-1757486919979.sql` - 데이터베이스 삽입 SQL

## 🎨 카드 특징

- **캐릭터**: Apple Scholar (귀여운 사과 캐릭터)
- **클래스**: Arcane Scholar (높은 지능에 맞는 마법사 타입)
- **스타일**: 한국 웹툰/만화 스타일, 브론즈 테마
- **능력치**: 김시온 학생의 실제 능력치 반영

## ✅ 완료 후 확인

카드 생성 후 김시온 학생이 다음에서 확인할 수 있습니다:
- 갤러리 페이지 (`/dashboard/gallery`)
- 프로필 페이지의 카드 섹션

## 🔄 향후 개선

나중에 실제 Gemini AI로 생성한 고품질 이미지로 교체 가능합니다.